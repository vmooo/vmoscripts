# vmoscripts
Some scripts for automationg daily tasks on Python and Bash

## Installation

Here is a bash script `install.sh` that finds all scripts, make them executable and creates symlinks in ~/.local/bin, so they can be invoked by name. To run `install.sh`, firstly make it executable
```bash
chmod +x install.sh
```
And run it
```bash
./install.sh
```